import{describe,expect,it}from'vitest';describe('custom tool copy',()=>{it('is independent and not built in',()=>{const source={id:'score',name:'Score Tracker',builtIn:true,sections:[{id:'a',label:'Score'}]};const copy={...structuredClone(source),id:'copy',builtIn:false,copiedFrom:source.name};copy.sections[0].label='Tables';expect(source.sections[0].label).toBe('Score');expect(copy.builtIn).toBe(false)})});
